# ITER-0002 progress

Links: [design](../../superpowers/specs/2026-08-17-iter-0002-identity-todo-conversation-loop-design.md) · [plan](../../superpowers/plans/2026-08-17-iter-0002-identity-todo-conversation-loop.md) · [brief](brief.md)

| Task | Status | Evidence |
| --- | --- | --- |
| 1 — Iteration ledger, policy refresh, and branch | Complete | Ledger + design + plan committed (`1937c53`); AGENTS.md zones refreshed; `.gitattributes` LF normalization (`7a5540b`); WSL gate env recorded (`f9f5bd5`); `wsl -d Ubuntu … make harness-test` exit 0. |
| 2 — Migrations 002–005, schema 1→5, and platform transaction capability | Complete | `go test ./backend/internal/platform/database -race` green; `wsl -d Ubuntu … make migration-test` exit 0 with `TestRunMigrationsTwice` (schema v5 + 10 tables) and `TestTxRunnerCommitRollbackAndNesting` PASS. |
| 3 — Platform server router evolution | Complete | `go test ./backend/internal/platform/server -race` green; new `RegisterHealthRoutes`/`NewAPIRouter` serve health routes with JSON 404/405 envelopes while `NewAPIHandler` stays byte-identical (its method-first 405 assertions remain green). |
| 4 — Identity domain and application | Complete | `go test ./backend/internal/modules/identity/... -race` green (domain invariants, login challenge/verify/logout with rate limit + single-use + hash-only codes, channels add/verify/enable, session Authenticate); `make architecture-test` green on the new module tree. |
| 5 — Identity outbound adapters (postgres + fake outbox) | Complete | `make migration-test` (WSL) exit 0; `TestUserStoreSaveAndByPhone`, `TestChallengeStoreLifecycle`, `TestSessionStoreByTokenHashAndRevoke`, `TestChannelStoreEnforcesUniqueKindAddress`, `TestOutboxWritesMessage` all PASS against live PostgreSQL; `backend-test` package list extended to `./backend/internal/modules/...`. |
| 6 — Identity HTTP, session middleware, dev inbox, config, and cmd wiring | Complete | Code landed in `f5db3ff`; accidentally tracked `api.exe` untracked and ignored (`d3dd58c`). Re-verified on the Linux dev host: `go test ./backend/internal/modules/identity/adapters/inbound/http ./backend/internal/platform/config -race` and `go build ./backend/cmd/api` green; `TEST_DATABASE_URL=… go test -p=1 ./backend/cmd/api ./backend/internal/modules/identity/adapters/outbound/... ./backend/internal/platform/database -race` green against live PostgreSQL 18.4 (`TestLoginRoundTripAndLogout`, `TestDevInboxAbsentWhenDisabled`, `TestHealthRoutesStillServed` PASS). |
| 7 — Reminder seam (domain, application, JobScheduler port, noop adapter) | Complete | `go test ./backend/internal/modules/reminder/... -race` green (domain invariants, plan/revoke handlers with recording fakes, noop scheduler); `TEST_DATABASE_URL=… go test ./backend/internal/modules/reminder/adapters/outbound/postgres -race` green against live PostgreSQL 18.4 (`TestPlanStoreSaveAndDuplicateIdempotency`, `TestPlanStoreRevokePlannedCutoff`, `TestPlanStoreJoinsAmbientTransaction` PASS); `make architecture-test` green on the new module tree (`3add142`). |
| 8 — Todo domain and application | Complete | `go test ./backend/internal/modules/todo/... -race` green (title rune bounds, optimistic version conflicts, terminal delete, ReminderVersion bump rules incl. clear/same-due, derived overdue; create/complete/delete/update with rollback fakes; list/get/dashboard/candidates incl. timezone-local window and zeroed reminder counters); `make architecture-test` green on the new module tree (`e5f66b0`). |
| 9 — Todo postgres adapter and atomic composition proof | Complete | `TEST_DATABASE_URL=… go test -p=1 ./backend/internal/modules/todo/adapters/outbound/postgres ./backend/cmd/api -race` green against live PostgreSQL 18.4: nullable round-trip, optimistic conflict, combinable filters with deleted excluded, dashboard counters on seeded fixtures, ILIKE candidates capped at 11, cross-workspace isolation; D1 atomicity proof `TestTodoReminderAtomicComposition` PASS (failing scheduler rolls back todo+plan, noop commits both at `scheduled_at_utc = due_at_utc`, complete revokes, reschedule yields revoked v1 + planned v2) (`88d4f2f`). |
| 10 — Todo HTTP inbound and dashboard endpoint | Complete | `go test ./backend/internal/modules/todo/adapters/inbound/http -race` green (auth-required registration, 201/422/409/404 envelopes, dueAtUtc present-vs-null PATCH semantics, filter parsing, dashboard timezone gating); cookie-authenticated cmd slice `TestTodoDashboardHttpRoundTrip` (create → list → complete → dashboard) PASS against live PostgreSQL 18.4; `make architecture-test` green (`f385d1f`). |
| 11 — Conversation domain and application (validation, router, clarification, confirmation) | Complete | `go test ./backend/internal/modules/conversation/... -race` green: confirmation bind/expire/consume at TTL boundary; every invalid proposal class rejected plus golden valid proposals per intent; router supports only registered intents; process-message branches (create echo incl. `2026-08-19 15:00` local echo, clarification gate, list filter mapping, delete 0/1/2/>10 branches, unsupported incl. injection-shaped proposals) and confirm-action flows (single-use, wrong scope, expired, stale todo version) (`138549d`). |
| 12 — Conversation model adapters (deterministic corpus + OpenAI-compatible) | Complete | `go test ./backend/internal/modules/conversation/adapters/outbound/... -race` green: 34 corpus lines pinned (incl. injection/bulk lines never dispatching), fixed confidence tiers, timezone-aware resolution with deterministic UTC fallback, byte-identical repeat output; OpenAI adapter proven against httptest for request shape, raw passthrough, typed 5xx/malformed errors, and context deadlines (`d22e5bd`). |
| 13 — Conversation HTTP inbound and full-path composition proof | Complete | `go test ./backend/internal/modules/conversation/adapters/... -race` green (conditional single-use consume incl. expired classification, scoped audit ordering/isolation, route auth + 422/404/409/410 envelope mapping); full-path composition `TestConversationEndToEndIntentPath` PASS over live PostgreSQL 18.4 with the deterministic adapter and cookie auth: create ⇒ `todo_created` + planned plan at due, list, delete ⇒ confirmation ⇒ `todo_deleted` + revoked plan + second confirm 409, unsupported text, and the 4-turn ordered audit trail (`c8a2e94`, D11). |
| 14 — OpenAPI contracts and contract tests | Complete | `go test ./tests/contract -race` green: four new OpenAPI 3.1.1 contracts (closed objects, exact required sets, enums, date-time formats, maxLengths, per-file ErrorEnvelope, dev inbox only in identity.yaml with the double-gating description) each with a traversal test and a mutation-rejection case; the system-health contract and its test untouched and still green (`ab26a4d`). |
| 15 — Web platform slice (rewrite proxy, session helper, app reorganization) | Pending | |
| 16 — Web features (auth, dashboard, todos, settings, conversation) | Pending | |
| 17 — Integration wiring (migration test list, Dockerfile, compose env, smoke E2E) | Complete | Backend test-stage CMD aligned with the gate package list; backend runtime gains tzdata (A1 zones); web image bakes `API_INTERNAL_URL` for the rewrite destination (D12); `APP_ENV`/`DEV_INBOX_ENABLED`/`MODEL_ADAPTER` wired into compose + `.env.example` with matching static assertions; `make migration-test` and `make smoke-test` both exit 0 on this host, including the ten-step authenticated E2E (login via dev inbox through the web rewrite, todo + reminder plan creation via conversation, confirmation-gated delete) and worker-stop degradation on `/status` (`ae91592`). |
| 18 — Unified verification, docs, handoff, and clean-context regression gate | Complete | Full CI sequence re-run green on the Linux host at the final tree: `corepack pnpm install --frozen-lockfile`, `make verify` (harness, format-check, lint, architecture-test, Go race tests + Web tests, builds, secrets), `make migration-test` (empty-schema gate, schema v5, adapter DB suite), `make smoke-test` (healthy/degraded/recovered plus the authenticated E2E loop); README routes/config and handoff refreshed; clean-context regression verdict recorded in `regression-report.md`. |

The independent clean-context regression re-ran the full gate sequence at `81e969f` and returned **PASS**; its immutable record is [regression-report.md](regression-report.md). ITER-0002 is complete.
