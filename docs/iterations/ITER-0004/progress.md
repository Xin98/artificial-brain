# ITER-0004 progress

Links: [design](../../superpowers/specs/2026-08-21-iter-0004-private-deployment-and-data-portability-design.md) · [plan](../../superpowers/plans/2026-08-21-iter-0004-private-deployment-and-data-portability.md) · [brief](brief.md)

| Task | Status | Evidence |
| --- | --- | --- |
| 1 — Iteration Ledger, Policy Refresh, and Branch | Complete | Ledger + AGENTS.md zone refresh committed (`3a094ed`); plan fixture amendment `c7e6cb8`; branch `iter-0004-private-deployment-and-data-portability` cut from `master` (`05dd59d`) after ITER-0003's passing regression; portability architecture-policy fixture added. |
| 2 — Migration 008 and Schema 7→8 (yellow) | Complete | `008_portability.sql` appended (`instance_meta`, `portability.portability_imports`, `portability.portability_source_records`, `reminder_deliveries.origin` defaulting `local`, nullable `plan_id`); `database.CurrentSchemaVersion` 7→8; in-place upgrade + idempotency integration test green (`a20175d`). |
| 3 — Platform Config Fields (yellow) | Complete | `DEPLOYMENT_MODE`, `PRIVATE_ADMIN_PHONE`, `PORTABILITY_MAX_BUNDLE_BYTES` with fail-closed validation (private mode requires the admin phone; bundle limit bounded); `go test ./backend/internal/platform/config -race` green (`805f539`). |
| 4 — Portability Domain — Manifest, Records, Decision Engine | Complete | `go test ./backend/internal/modules/portability/domain -race` green: manifest/record validation, four-outcome decision engine (new/skipped/conflict/invalid), stable content fingerprints (`5e0aaeb`). |
| 5 — Identity Seams — Provision Admin, Login Gate, Import/Export Channels (green) | Complete | `go test ./backend/internal/modules/identity/... -race` green: idempotent `ProvisionAdmin`, application-layer login gate (`ErrRegistrationClosed` when another phone logs in under a configured `PrivateAdminPhone`), unverified channel import, channel preference export without codes or verification state (`a437a7c`). |
| 6 — Todo Seams — Restore Constructor, Import/Export (green) | Complete | `go test ./backend/internal/modules/todo/... -race` green: `Restore` preserving status and timestamps, `ImportTodo` creating no reminder plan or job, full-history `ExportTodos` paging (`90753a0`). |
| 7 — Reminder Seams — Imported Deliveries and Export (green) | Complete | `go test ./backend/internal/modules/reminder/... -race` green: `RestoreDelivery` with `origin`, read-only `ImportDeliveries` history (NULL plan_id, idempotency key `import:<sourceInstanceId>:<sourceRecordId>`, no scheduling or provider side effects), delivery export with `origin` (`08c298d`). |
| 8 — Portability Application — Ports, DTOs, and ExportBundle | Complete | `go test ./backend/internal/modules/portability/application -race` green: consumer-owned ports, bundle DTOs, streaming `ExportBundle` with per-entry sha256 tee and manifest-last writing (`d1c93df`). |
| 9 — Portability Application — Upload, Preview, Confirm | Complete | `go test ./backend/internal/modules/portability/application -race` green: upload stores the validated bundle with a pure-computation preview; confirm re-decides from stored bytes and executes channels → todos → deliveries in one transaction exactly once; source-record fingerprint/target resolution (`9bf0067`). |
| 10 — Portability Postgres Adapters — Imports, Source Records, Instance Meta | Complete | `TEST_DATABASE_URL=… go test ./backend/internal/modules/portability/adapters/outbound/postgres -race -p=1` green: import store (bytea bundle + preview column), source-record register/fingerprints/targets, instance meta; migration 008 gains the preview column (`7dadb92`). |
| 11 — Portability Archive Adapter — Zip Streaming, Parse, CSV | Complete | `go test ./backend/internal/modules/portability/adapters/outbound/archive -race` green: streaming zip writer, parser with entry-set and manifest validation, human-readable CSV encoding (`3d9a956`). |
| 12 — Portability HTTP Inbound — Export, Upload, Confirm, Get | Complete | `go test ./backend/internal/modules/portability/adapters/inbound/http -race` green: export zip stream with attachment headers, size-limited multipart upload, import view, confirm; stable codes `bundle_too_large`/`bundle_invalid`/`unsupported_schema_version`/`checksum_mismatch` (422), 404, `import_conflict` (409) (`4e12190`). |
| 13 — Composition Root — Provisioning and Portability Wiring (yellow) | Complete | `cmd/api` wires portability routes/stores/archive and the startup provisioning chain (schema gate, instance identity, private admin); composition tests green. Follow-up fix maps `ErrRegistrationClosed` → 403 `registration_closed` on both login routes (`115ce38`, `1aa697e`). |
| 14 — Contracts — OpenAPI and Export Schemas (yellow) | Complete | `contracts/openapi/portability.yaml` (routes 27–30) + `contracts/export-schemas/**` (manifest, todos, preferences, reminder-deliveries) validated against good/bad sample bundles; `go test ./tests/contract -race` green (`be09f4c`). |
| 15 — Web — /data Page With Export and Import Flow (green) | Complete | `corepack pnpm --filter @artificial-brain/web test` green: `/data` page with export download, three-step import state machine (upload → preview → report), actionable error copy per stable code, truncation note (`6592db2`). |
| 16 — Private Deployment Assets, Backup/Restore, Offline Bundle (yellow) | Complete | `deploy/private/{README.md,backup.sh,restore.sh,env.template}` + `docs/runbooks/{backup-restore,upgrade}.md`; `make backup`/`make restore`/`make offline-bundle`; compose env passthrough + `.env.example`; `make harness-test`, `sh tests/smoke/stack_test.sh --static-only`, and `docker compose config --quiet` green (`42936fc`). |
| 17 — Smoke Gates — Portability, Private Mode, Backup/Restore, Upgrade Drill (yellow) | Complete | Smoke gains the four blocks; `migration_test.sh` pinned to schema v8; `make migration-test` ×2 and `make smoke-test` ×2 exit 0. Includes three out-of-scope regression fixes (empty-schema fail-safe, transaction-clean channel import downgrade, nullable plan scans) committed separately (`5b8b752` + `4e94f7e`). |
| 18 — Unified Verification, Docs, Handoff, and Clean-Context Regression Gate | Complete | Full CI sequence green at the branch tip `4e94f7e` (2026-08-22): `corepack pnpm install --frozen-lockfile` exit 0, `make verify` exit 0, `make migration-test` exit 0 (schema v8), `make smoke-test` exit 0. Independent clean-context regression **PASS** (`regression-report.md`, `17714d9`): all four gates re-ran exit 0, 10/10 acceptance criteria mapped to re-verified evidence, zero new dependencies, migrations 001–007 untouched, no real-provider egress in CI, no committed credentials, architecture green; out-of-scope fixes (`5b8b752`, O1–O6) audited hunk-by-hunk with no reach-beyond. |

All 18 tasks are complete. The unified verification sequence (`corepack pnpm install --frozen-lockfile`, `make verify`, `make migration-test`, `make smoke-test`) ran clean at the branch tip with every command exiting 0, and the independent clean-context regression returned **PASS** (`regression-report.md`). ITER-0004 is iteration-complete and ready for merge review.
