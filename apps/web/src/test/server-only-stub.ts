// Test-only stub for the "server-only" package, aliased in vitest.config.ts.
// It lets modules guarded for server use be imported by unit tests without
// weakening the production guard.
export {};
