# ITER-0001 test matrix

| Requirement                                                  | Command and evidence                                                                                                                                                                                                | Evidence commit                | Status                       |
| ------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------ | ---------------------------- |
| Pinned, reproducible toolchain                               | `make toolchain-check`; the harness also exercises accepted and rejected fixture versions                                                                                                                           | `47ed457`                      | Passed                       |
| Clean repository policy                                      | `make harness-test`; rejects tracked `.DS_Store` and `.env`                                                                                                                                                         | `c8d3594`                      | Passed                       |
| CFG-01 — API configuration defaults                          | `go test ./backend/internal/platform/config -race`; `TestLoadValidRoles/api` verifies role, service, address, and shared defaults                                                                                   | `56922b9`                      | Passed                       |
| CFG-02 — Worker configuration defaults                       | `go test ./backend/internal/platform/config -race`; `TestLoadValidRoles/worker` verifies role, service, address, and shared defaults                                                                                | `56922b9`                      | Passed                       |
| CFG-03 — Migrate configuration defaults                      | `go test ./backend/internal/platform/config -race`; `TestLoadValidRoles/migrate` verifies role, service, and shared defaults                                                                                        | `56922b9`                      | Passed                       |
| CFG-04 — Worker duration overrides                           | `go test ./backend/internal/platform/config -race`; `TestLoadWorkerConfig` verifies heartbeat and lease durations                                                                                                   | `56922b9`                      | Passed                       |
| CFG-05 — Invalid configuration is rejected                   | `go test ./backend/internal/platform/config -race`; `TestLoadRejectsInvalidConfiguration` covers missing database URL, invalid durations, lease constraint, and unknown roles                                       | `56922b9`                      | Passed                       |
| CFG-06 — Configuration errors preserve secrets               | `go test ./backend/internal/platform/config -race`; `TestLoadNeverEchoesSecretValue` proves errors do not expose database credentials                                                                               | `56922b9`                      | Passed                       |
| CFG-07 — Role-specific HTTP address overrides                | `go test ./backend/internal/platform/config -race`; `TestLoadUsesRoleSpecificHTTPAddressOverrides` proves role ownership, defaults, and Migrate isolation                                                           | `2e63287`                      | Passed                       |
| OBS-01 — Correlation ID context seam                         | `go test ./backend/internal/platform/observability -race`; `TestCorrelationIDRoundTrip` verifies context storage and retrieval                                                                                      | `56922b9`                      | Passed                       |
| OBS-02 — Empty correlation IDs stay absent                   | `go test ./backend/internal/platform/observability -race`; context and logger tests verify absence is preserved                                                                                                     | `56922b9`                      | Passed                       |
| OBS-03 — Structured logger fields                            | `go test ./backend/internal/platform/observability -race`; logger tests verify JSON service, version, message, level, time, and correlation fields                                                                  | `56922b9`                      | Passed                       |
| DB-01 — Pool configuration                                   | `go test ./backend/internal/platform/database -race`; pool tests verify parsing, five-second connect timeout, ping, and failed-ping close                                                                           | `0ca5418`                      | Passed                       |
| DB-02 — Consumer-owned schema query seam                     | `go test ./backend/internal/platform/database -race`; `Queryer` exposes only `QueryRow`                                                                                                                             | `0ca5418`                      | Passed                       |
| DB-03 — Compatible schema accepted                           | `go test ./backend/internal/platform/database -race`; schema tests accept version 1                                                                                                                                 | `0ca5418`                      | Passed                       |
| DB-04 — Absent schema rejected safely                        | `go test ./backend/internal/platform/database -race`; absent row maps to `ErrSchemaIncompatible`                                                                                                                    | `0ca5418`                      | Passed                       |
| DB-05 — Mismatched schema rejected safely                    | `go test ./backend/internal/platform/database -race`; mismatch is redacted and maps to `ErrSchemaIncompatible`                                                                                                      | `0ca5418`                      | Passed                       |
| MIG-01 — One-shot migration ownership                        | `go test ./backend/cmd/migrate ./backend/internal/platform/database -race`; only Migrate invokes `RunMigrations`                                                                                                    | `c882e07`                      | Passed                       |
| MIG-02 — Schema version migration                            | `make migration-test`; isolated PostgreSQL reaches `public.schema_version = 1`                                                                                                                                      | `417240e`                      | Passed live PostgreSQL proof |
| MIG-03 — Runtime Worker schema                               | `make migration-test`; verifies exactly one `runtime.worker_heartbeats` table                                                                                                                                       | `417240e`                      | Passed live PostgreSQL proof |
| MIG-04 — Idempotent migration execution                      | `make migration-test`; Migrate runs twice and the database integration test repeats under `-race`                                                                                                                   | `417240e`                      | Passed live PostgreSQL proof |
| WKR-01 — Worker lease registry                               | `make migration-test`; registry integration covers ordering, upsert preservation, and removal under `-race`                                                                                                         | `417240e`                      | Passed live PostgreSQL proof |
| WKR-02 — Deterministic heartbeat lifecycle                   | `go test ./backend/internal/platform/workerstatus -race`; channel-backed ticks prove immediate/tick record, bounded cleanup, removal, and tick stopping                                                             | `3109362`                      | Passed                       |
| WKR-03 — Heartbeat failures                                  | `go test ./backend/internal/platform/workerstatus -race`; record and cancellation-cleanup failures propagate deterministically                                                                                      | `3109362`                      | Passed                       |
| HLT-01 — Deterministic system health                         | `go test ./backend/internal/platform/systemhealth -race`; fixed keys, aggregation, redaction, and correlation propagation are covered                                                                               | `1991369`                      | Passed                       |
| HLT-02 — Worker lease boundary handling                      | `go test ./backend/internal/platform/systemhealth -race`; TTL equality, expiration, and negative clock skew are covered                                                                                             | `1991369`                      | Passed                       |
| HLT-03 — Versioned health OpenAPI contract                   | `go test ./tests/contract -race`; typed parsing traverses required routes, schemas, strict shapes, detail limit, and enums                                                                                          | `1991369`                      | Passed                       |
| HLT-04 — Contract invalid-document resistance                | `go test ./tests/contract -race`; malformed YAML, comment-only routes, missing schemas, and misplaced enums are rejected                                                                                            | `1991369`                      | Passed                       |
| API-01 — Correlation middleware                              | `go test ./backend/internal/platform/server -race`; valid, absent, invalid, and entropy-failure cases are covered                                                                                                   | `ab561f1`                      | Passed                       |
| API-02 — API health transport                                | `go test ./backend/internal/platform/server -race`; route JSON, correlations, degraded reports, and stable method/path errors are covered                                                                           | `ab561f1`                      | Passed                       |
| API-03 — API lifecycle                                       | `go test ./backend/internal/platform/server -race`; cancellation triggers graceful shutdown through a real loopback listener                                                                                        | `ab561f1`                      | Passed                       |
| WKR-04 — Worker heartbeat readiness state                    | `go test ./backend/internal/platform/server -race`; state requires a successful heartbeat and resets after failure                                                                                                  | `2e63287`                      | Passed                       |
| WKR-05 — Worker health transport                             | `go test ./backend/internal/platform/server -race`; liveness, database/heartbeat readiness, redaction, correlation, and GET-only behavior are covered                                                               | `2e63287`                      | Passed                       |
| WKR-06 — Worker process composition                          | `go build ./backend/cmd/worker`; typed config, schema gate, IDs, heartbeat/server lifecycle, and shutdown wiring compile                                                                                            | `2e63287`                      | Passed                       |
| WEB-01 — Strict degradable health client                     | `corepack pnpm --filter @artificial-brain/web test`; client tests cover valid, non-2xx, malformed, timeout, canonical URL, redaction, and fallback behavior                                                         | `f5eb3a6`                      | Passed                       |
| WEB-02 — Accessible four-card system-health view             | `corepack pnpm --filter @artificial-brain/web test`; view tests cover healthy, degraded, unavailable, four labels, and internal-detail omission                                                                     | `f5eb3a6`                      | Passed                       |
| WEB-03 — Server health page and liveness route               | `corepack pnpm --filter @artificial-brain/web build`; standalone output contains server-rendered `/` and `/health/live`                                                                                             | `f5eb3a6`                      | Passed                       |
| CNT-01 — Pinned non-root backend images                      | `sh tests/smoke/stack_test.sh --static-only`; verifies Go build/test stages, three static binaries, pinned runtime, CA bundle, and non-root UID                                                                     | `417240e`                      | Passed                       |
| CNT-02 — Pinned standalone Web image                         | `sh tests/smoke/stack_test.sh --static-only`; verifies Node pin, frozen install, standalone/static copy, and non-root runtime                                                                                       | `417240e`                      | Passed                       |
| CMP-01 — Ordered private Compose topology                    | `sh tests/smoke/stack_test.sh --config-only`; validates exact services, PostgreSQL pin, conditions, private ports, test profile, read-only migrations, and volume                                                   | `417240e`                      | Passed                       |
| OPS-01 — Stable runtime Make interface and destructive guard | `make -n dev down build migration-test smoke-test clean-local-data CONFIRM=delete`; missing delete confirmation is rejected before Docker                                                                           | `417240e`                      | Passed                       |
| OPS-02 — Isolated and bounded smoke execution                | `sh tests/smoke/stack_test.sh --redaction-test` plus smoke self-tests; project isolation, deadlines, and credential redaction are covered                                                                           | `417240e`                      | Passed                       |
| OPS-03 — Complete read-only verification graph               | `make harness-test` and `make -n verify`; all final targets resolve, verify selects `format-check`, and no mutating formatter is selected                                                                           | `6ba6de0`                      | Passed                       |
| OPS-04 — Tracked-file credential policy                      | `make harness-test` and `sh scripts/check-secrets.sh`; private-key headers, token prefixes, and live-looking database credentials fail with redacted diagnostics, while allowed fixtures and untracked files do not | `6ba6de0`                      | Passed                       |
| CI-01 — Unified ordered CI entry points                      | `go test ./tests/harness -run TestWorkflow -count=1`; typed YAML traversal proves executable verify, migration, and smoke steps in order                                                                            | `6ba6de0`                      | Passed                       |
| Web/API/Worker health chain                                  | `make smoke-test`; Docker-assigned ports prove healthy → degraded after Worker stop → healthy after restart, including API and four-card Web                                                                        | `417240e`                      | Passed live Compose proof    |
| Migration ownership                                          | `make migration-test`; proves API/Worker do not create schema, Migrate is idempotent, and exact objects exist                                                                                                       | `417240e`                      | Passed live Compose proof    |
| Dependency direction                                         | `make architecture-test`; fixtures and the real tree enforce import and deployment-reference policy                                                                                                                 | `742d85c`                      | Passed                       |
| Clean-context regression                                     | `make verify && make migration-test && make smoke-test`; immutable reviewer report is `regression-report.md`                                                                                                        | `760e4c6`                      | Passed independent regression |
