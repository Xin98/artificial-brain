# ITER-0001 progress

Links: [design](../../superpowers/specs/2026-08-13-iter-0001-runnable-skeleton-design.md) · [plan](../../superpowers/plans/2026-08-13-iter-0001-runnable-skeleton.md) · [brief](brief.md)

| Task | Status | Evidence |
| --- | --- | --- |
| 1 — Repository policy, toolchain pins, and iteration ledger | Complete | `make toolchain-check` passed with the exact pinned Go 1.26.5, Node.js 24.18.0, and pnpm 11.19.0 versions. |
| 2 — Typed configuration and structured observability | Complete | `GOCACHE=/private/tmp/artificial-brain-task2-go-build GOMODCACHE=/private/tmp/artificial-brain-task2-go-mod go test ./backend/internal/platform/config ./backend/internal/platform/observability -v` |
| 3 — PostgreSQL connection, one-shot migration, and schema compatibility | Complete | Schema tests and database/command package tests pass; migration integration skips only while `TEST_DATABASE_URL` is absent. |
| 4 — Worker lease registry and deterministic heartbeat loop | Complete | Registry integration test skips only while `TEST_DATABASE_URL` is absent; heartbeat lifecycle tests pass under `-race`, including immediate record, channel-backed ticks, cancellation cleanup, record failure, cleanup failure, and tick stopping. |
| 5 — System health module and versioned HTTP contract | Complete | `GOCACHE=/private/tmp/artificial-brain-task5-go-build GOMODCACHE=/private/tmp/artificial-brain-go-mod go test ./backend/... ./tests/... -race` passed; typed contract traversal validates OpenAPI 3.1.1 routes and response schemas. |
| 6 — Correlation middleware, stable errors, and API process | Complete | `GOCACHE=/private/tmp/artificial-brain-task6-go-build GOMODCACHE=/private/tmp/artificial-brain-go-mod go test ./backend/... -race` and `go build ./backend/cmd/api` passed. |
| 7 — Worker health probes and Worker process composition | Complete | Worker state and health-handler tests pass under `-race`; `go build ./backend/cmd/worker` passes. Controller approved the minimal Task 2 configuration gap: API and Worker now use their role-specific address overrides. |
| 8 — Next.js health client and degradable status page | Complete | `CI=true corepack pnpm --filter @artificial-brain/web format:check`, `test`, `lint`, and `build` pass; the production build emits the standalone output. The exact `@eslint/compat` 2.1.0 layer preserves the full Next/React/TypeScript lint rules under ESLint 10. |
| 9 — Executable architecture policy with positive and negative fixtures | Complete | `GOCACHE=/private/tmp/artificial-brain-task9-go-build GOMODCACHE=/private/tmp/artificial-brain-go-mod go test ./architecture/... -race` and `make architecture-test` pass; deterministic fixture and real-tree validation enforce dependency direction. |
| 10 — Reproducible images, ordered Compose runtime, and smoke tests | Complete | `sh tests/smoke/stack_test.sh --config-only`, `make migration-test`, and `make smoke-test` pass against unique isolated Colima projects even when unsafe ambient project/port variables are exported. The live proof includes bounded empty-schema API/Worker ownership checks, idempotent migrations, PostgreSQL adapter tests under `-race`, Docker-assigned smoke ports, all five services healthy, Worker lease degradation, Web degraded rendering, and recovery after Worker restart. Exact JSON/key-value/URI redaction tests, CA image assertions, bounded URL polling, guarded cleanup, and `CI=true make build` also pass. |
| 11 — Unified verification, CI, run documentation, and clean handoff | Complete | Behavior tests exercise the complete Make graph with `make -n`; the workflow is parsed through `yaml.v3`; secret self-tests prove tracked-only scanning, narrow placeholder/interpolation handling, and redacted rejection. `CI=true corepack pnpm install --frozen-lockfile`, `CI=true make verify`, `CI=true make migration-test`, and `CI=true make smoke-test` are the final implementation-context gates. The secret policy treats only explicit placeholder examples and exact Compose interpolation as non-credentials; a live-looking tracked URL remains a violation. |

Independent clean-context regression passed on 2026-08-14 against `760e4c6`; see [regression-report.md](regression-report.md). All three gates (`make verify`, `make migration-test`, `make smoke-test`) exited 0.
