# ITER-0004 test matrix

Rows are recorded as each task reaches green. IDs group by subsystem: `CFG` platform config, `MIG` migrations/schema, `PDM` portability domain, `IDT` identity seams, `TDO` todo seams, `RMD` reminder seams, `PTA` portability application, `HTP` portability HTTP, `CNT` contracts, `WEB` web, `DEP` deploy assets, `SMK` smoke gates.

| Requirement | Command and evidence | Evidence commit | Status |
| --- | --- | --- | --- |
| MIG-01 — schema advances 7→8 via append-only migration 008 (instance meta, portability imports, source records, delivery origin, nullable plan_id) | `make migration-test`; empty-schema gate, idempotent re-run, `schema_version=8` assertion, `public.instance_meta` + `portability.portability_imports` + `portability.portability_source_records` exist, `reminder.reminder_deliveries.origin` defaults to `local` and `plan_id` is nullable | — | Pending |
| CFG-01 — deployment mode, private admin phone, and bundle limit configuration with fail-closed validation | `go test ./backend/internal/platform/config -race` | — | Pending |
| PDM-01 — portability domain: manifest and record validation, four-outcome decision engine, stable content fingerprints | `go test ./backend/internal/modules/portability/domain -race` | — | Pending |
| IDT-01 — identity seams: idempotent private admin provisioning, application-layer login gate (`registration_closed`), unverified channel import, channel preference export without codes or verification state | `go test ./backend/internal/modules/identity/... -race` | — | Pending |
| TDO-01 — todo seams: `Restore` constructor preserving status and timestamps, `ImportTodo` creating no reminder plan or job, full-history `ExportTodos` paging | `go test ./backend/internal/modules/todo/... -race` | — | Pending |
| RMD-01 — reminder seams: `RestoreDelivery` with `origin`, read-only `ImportDeliveries` history (NULL plan_id, no scheduling or provider side effects), delivery export | `go test ./backend/internal/modules/reminder/... -race` | — | Pending |
| PTA-01 — portability application: streaming ExportBundle (per-file sha256 tee, manifest last), upload stores the validated bundle with a pure-computation preview, confirm re-decides from the stored bytes and executes channels → todos → deliveries in one transaction exactly once | `go test ./backend/internal/modules/portability/application -race` | — | Pending |
| HTP-01 — portability HTTP: export zip stream with attachment headers, size-limited multipart upload, import view, confirm; stable codes `bundle_too_large`/`bundle_invalid`/`unsupported_schema_version`/`checksum_mismatch` (422), 404, and `import_conflict` (409) | `go test ./backend/internal/modules/portability/adapters/inbound/http -race` | — | Pending |
| CNT-01 — portability OpenAPI contract (routes 27–30) and export bundle schemas validated against good/bad sample bundles | `go test ./tests/contract -race` | — | Pending |
| WEB-01 — `/data` page: export download, three-step import state machine (upload → preview → report), actionable error copy per stable code, truncation note | `corepack pnpm --filter @artificial-brain/web test` | — | Pending |
| DEP-01 — private deployment assets: `deploy/private/**` scripts with CONFIRM gate, `make backup`/`make restore`/`make offline-bundle`, compose env passthrough, static smoke env assertions | `make harness-test`; `sh tests/smoke/stack_test.sh --static-only`; `docker compose config --quiet` | — | Pending |
| SMK-01 — smoke gates: portability block (export → re-import all skipped → conflict on change), private-mode block (admin login, other phone rejected), backup/restore block, same-version upgrade drill block | `make smoke-test` | — | Pending |
| Clean-context regression | `make verify && make migration-test && make smoke-test` re-run independently plus acceptance mapping, zone compliance (zero new dependencies, migrations 001–007 untouched, no real-provider egress in CI), and credential scan; immutable reviewer report is `regression-report.md` | — | Pending |
