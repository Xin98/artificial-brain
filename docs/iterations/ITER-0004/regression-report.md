# ITER-0004 clean-context regression report

- Branch: `iter-0004-private-deployment-and-data-portability`
- Range reviewed: merge-base `05dd59d` → HEAD `17714d9` (23 commits; verified via `git merge-base HEAD master`, working tree clean)
- Date: 2026-08-22
- Reviewer: independent clean-context regression agent (fresh context, no implementation knowledge)

## 1. Reviewer scope statement

This review was performed by a fresh agent with **no knowledge of the implementation conversation**. Inputs used, and only these:

1. Approved design spec `docs/superpowers/specs/2026-08-21-iter-0004-private-deployment-and-data-portability-design.md` (acceptance criteria §14).
2. Iteration ledger `docs/iterations/ITER-0004/{brief,spec,plan,decisions,progress,test-matrix,handoff}.md`.
3. Implementation plan `docs/superpowers/plans/2026-08-21-iter-0004-private-deployment-and-data-portability.md`.
4. The branch diff `05dd59d..17714d9` (navigated via `git log/diff/show` on the range).
5. `README.md` (Prerequisites, Verification) and the `Makefile`.

What the reviewer did: re-ran the full acceptance sequence at the branch tip in order, recording each exit code; mapped each §14 criterion to concrete evidence; re-ran the secret scanner; verified zone/compliance constraints against the diff; audited the documented out-of-scope fixes against their hunks; ran one supplementary targeted test filter (web `features/data`).

What the reviewer did NOT do: read any implementation chat or transcript; modify any source, test, config, or migration file (this report is the only write); give any benefit of the doubt — every PASS below names its evidence.

Environment notes: Go 1.26.5, Node v24.18.0, pnpm 11.19.0, Docker running; the `ab-test-postgres` container was already up (host port 5433). One deviation from the dispatch brief: `TEST_DATABASE_URL` was **not** present in the reviewer's shell despite the brief stating it would be; the sequence was therefore run exactly as CI runs it (README documents that DB-backed integration tests are skipped when `TEST_DATABASE_URL` is unset). All DB-backed evidence was supplied by `make migration-test`, whose compose `backend-test` proof executes the integration and composition suites for real (see gate 3 evidence). This mirrors the ITER-0003 report's finding that exporting `TEST_DATABASE_URL` into `make verify` with default package parallelism is an invocation artifact, not the documented procedure.

## 2. Gate results

| # | Command (branch tip `17714d9`) | Exit code | Evidence |
|---|---|---|---|
| 1 | `corepack pnpm install --frozen-lockfile` | **0** | "Already up to date", Done in 652ms |
| 2 | `make verify` | **0** | harness-test, format-check (prettier), lint (`go vet` + eslint + `tsc --noEmit`), architecture-test (incl. `TestValidateFixtures/portability_imports_another_context's_domain`), `go test ./... -race` (50 `ok` packages; DB integration tests skipped — CI mode), web vitest **18 files / 106 tests passed**, `go build` + `next build` (route table includes `/data`), `check-secrets.sh` |
| 3 | `make migration-test` | **0** | Isolated empty-schema stack reached `schema_version=8`; adapter DB proof green, including the real-DB composition tests `TestPrivateModeProvisioningAndLoginGate`, `TestProvisionInstanceIdentityIsStable`, and `TestPortabilityRoundTripBetweenTwoWorkspaces` (all PASS in the log); all ITER-0001–0003 suites re-ran green |
| 4 | `make smoke-test` | **0** | Full Compose proof incl. legacy e2e loop and all four ITER-0004 blocks; log shows the private project booting and tearing down, `backup: archive /tmp/.../backup-20260821T180824Z.dump`, `restore: restored ... into project ...`, and the post-restore rebuild (`compose up --build --force-recreate`); `SMOKE_EXIT_CODE=0` |

Supplementary (not part of the sequence): `corepack pnpm --filter @artificial-brain/web test src/features/data` → 3 files / 28 tests passed; `sh scripts/check-secrets.sh` → exit 0.

## 3. Acceptance-criteria mapping (design §14)

| # | Criterion | Status | Evidence |
|---|---|---|---|
| 1 | Private mode: fixed admin two-step login; other phones `registration_closed`; idempotent provision; `config.Load` fails without `PRIVATE_ADMIN_PHONE` | **PASS** | Config: `TestLoadPrivateModeRequiresAdminPhoneForAPIRole` (+ invalid-phone, unknown-mode, cloud-rejects-phone cases) in `config_iter0004_test.go`. Provision: `provision_test.go` (saves workspace-then-user; existing user no-op) + composition `TestPrivateModeProvisioningAndLoginGate` (real DB, ran in gate 3). Gate: `TestRequestLoginChallengeGateRejectsOtherPhonesBeforeAnyStore` / `Verify…` twins; HTTP 403 mapping `TestPrivateModeLoginGateMapsTo403RegistrationClosed` (fix `1aa697e`, documented O2). End-to-end: smoke private block — admin `+8613800137999` request 202 → dev-inbox code → verify sets `ab_session` → dashboard renders; stranger `+8613800137998` → 403 body code `registration_closed` (gate 4 log, exit 0) |
| 2 | Cloud mode identical to ITER-0003 (no regression across login/todo/conversation/reminder) | **PASS** | Gate 2 runs the entire pre-existing suite green (identity, todo, conversation, reminder modules; ITER-0002/0003 composition tests re-ran in gate 3); gate 4 legacy smoke blocks (auth loop, todos, conversation, reminder delivery/suppression/receipts/ops, worker-restart recovery) all precede the ITER-0004 blocks and passed (script fails fast on any block); login gate tests assert empty `PrivateAdminPhone` keeps behavior unchanged; compose defaults stay `cloud` + fake adapters |
| 3 | `POST /api/v1/portability/export` returns valid zip: manifest version/source/counts/checksums, four data files, sha256 self-consistent, human-readable CSV, no codes/sessions/keys/provider credentials | **PASS** | Smoke: export 200, `unzip -Z1` lists exactly the five entries, `manifest.json` `schemaVersion == "1"` and `counts.todos >= 1` asserted. Unit: `writer_test.go` (entry order, per-entry sha256 tee, manifest-last, hashes match bytes), `csv.go` golden rows. Contract: `contracts/export-schemas/*` validated by `export_bundle_contract_test.go` (good/bad samples, ran in gate 2). No-secrets: `TestGetChannelPreferencesReturnsOnlyPreferenceFields` proves the exported channel shape is id/kind/address/enabled only — `Verified`, `CodeHash`, `CodeExpiresAt` dropped; bundle carries no session, code, key, or provider-credential records by construction (todos, delivery audit, channel preferences) |
| 4 | Upload returns preview with correct new/skipped/conflict/invalid classification; preview writes no business data | **PASS** | `UploadImportHandler` holds only `Imports`/`Sources`/`Parser` ports — no importer is reachable at upload; the only write is the portability bookkeeping row (bundle + preview). Tests: `TestUploadImportHappyPathDecidesPerKindAgainstExistingFingerprints` (mixed new/skipped/conflict), `TestUploadImportMarksInvalidRecordsAndKeepsThemOutOfDecide`, `TestUploadImportTruncatesDetailsAtHundredPerOutcome`, `TestUploadImportZeroRecordBundleStillStoresEmptyPreview`. Smoke: first upload → 201, preview `new == todos+deliveries+channels`, all other buckets 0 |
| 5 | Confirm executes channels→todos→deliveries; imported todos carry no reminder plan; imported channels unverified; deliveries `origin=imported` with no delivery side effects; re-confirm after committed → 409 + existing report, no re-execution | **PASS** | Order: `TestConfirmImportHappyPathExecutesChannelsTodosDeliveriesInOrder` (recording fakes). No plans: composition `TestPortabilityRoundTripBetweenTwoWorkspaces` (real DB: copied todo has zero `reminder_plans` rows, channel unverified) + smoke asserts `reminder.reminder_plans` count unchanged by the import. Deliveries: smoke asserts `origin = 'imported'` row count equals bundle deliveries and `/api/v1/reminders` shows preserved states; reminder seam tests assert `ImportDeliveries` touches no scheduler/provider. Re-confirm: `TestConfirmImportReconfirmAfterCommitReportsConflictWithoutTouchingImporters` + smoke re-confirm of the committed importId → 409 `import_conflict`; the stored report remains retrievable and the smoke then reads `GET imports/{id}` asserting `state == "committed"` with the report (`report.new` matches). Note: the 409 body itself is the frozen contract's `ErrorEnvelope` (§7.1 route table, `portability.yaml` 409 schema); design §7.2's prose "409 附既有报告" is satisfied through the stored, GET-retrievable report — see §5 observations |
| 6 | Re-import of the same bundle is all skipped, no duplicate records (scenario 10) | **PASS** | Smoke: re-upload → 201 preview `skipped == total`, re-confirm → 200 report `new == 0`, `skipped == total`, todo count unchanged before/after. Unit: `TestDecideSameFingerprintIsSkipped`, fingerprint stability tests |
| 7 | Same source identity with changed content ⇒ conflict: skipped, reported, original DB row untouched | **PASS** | Domain: `TestDecideChangedFingerprintIsConflict`. Preview: `upload_test.go` asserts `Conflicts: 1` with reason "fingerprint changed since last import". Execution: `confirm.go` `execute()` invokes importers **only** for `OutcomeNew` decisions — conflict/skipped/invalid records are never written and their source records never re-registered, so the original row cannot change; the conflict lands in the report via `buildReport(plan.decisions)` |
| 8 | Bad bundles (oversized, broken structure, unsupported version, checksum mismatch, invalid records) rejected with stable codes; no half-imported state | **PASS** | Parser: `TestParseMissingEntryReportsBundleStructure`, `TestParseUnexpectedEntryReportsBundleStructure`, `TestParseUnsupportedSchemaVersion`, `TestParseCorruptedEntryReportsChecksumMismatch`, `TestParseInvalidRecordReportsRecordInvalid`, `TestParseNonZipReportsBundleStructure`. HTTP: `TestUploadMapsTypedParserErrorsToStableCodes`, `TestUploadRejectsOversizedBundleWithoutCallingParser`, `TestUploadRejectsBadMultipartAsBundleInvalid`. Smoke end-to-end: garbage upload → 422 `bundle_invalid`; dd-padded upload past the 1 MiB smoke cap → 422 `bundle_too_large`. No half-import: upload fails fast before storing; confirm runs inside one unit of work — `TestConfirmImportImporterFailureFailsTheTransactionAndKeepsStatePending` (and channel/delivery/register-failure twins) prove rollback leaves `state=pending` |
| 9 | `make backup` produces archive + sha256; `make restore BACKUP=… CONFIRM=restore` restores intact data; without CONFIRM it refuses | **PASS** | `backup.sh`: `pg_dump --format=custom` → timestamped `.dump` + `.sha256` sidecar; `restore.sh`: refuses unless `CONFIRM=restore`, verifies the sha256 sidecar when present, stops api/worker/web, `pg_restore --clean --if-exists`, restarts. Makefile guards (`make restore` requires `BACKUP` and `CONFIRM=restore`). Smoke: backup archive produced → copy todo deleted via confirmation flow → restore → row back (`deleted_at is null` count 1), dashboard answers, `/status` healthy; `CONFIRM=wrong` run exits non-zero and the database is verified untouched |
| 10 | Smoke contains upgrade drill (write → recreate → idempotent migrate → data intact) and private-mode blocks; `make verify`/`migration-test`/`smoke-test` all green; zero new go.mod/web deps; ledger complete; independent regression PASS | **PASS** | Upgrade block: counts recorded → `compose up --build --force-recreate` → migrate exit 0 → `schema_version = 8` → todo/delivery counts unchanged → `/status` healthy. Private block present (see criterion 1). Gates: §2 table, all exit 0. Dependencies: `git diff 05dd59d..HEAD -- go.mod go.sum apps/web/package.json pnpm-lock.yaml` is **empty**. Ledger: all seven files present and consistent. This report is the independent regression |

**Tally: 10 PASS / 0 PARTIAL / 0 FAIL.**

## 4. Compliance findings

- **Zero new runtime dependencies.** `git diff 05dd59d..HEAD -- go.mod go.sum apps/web/package.json` produced no output at all (no additions, no version churn); `pnpm-lock.yaml` likewise untouched. The zip/sha256/csv/multipart surface is Go stdlib as the design required.
- **Migrations 001–007 untouched; only 008 added.** `git diff 05dd59d..HEAD -- deploy/migrations/` shows exactly one new file, `008_portability.sql` (append-only with a matching down section); `git ls-tree HEAD deploy/migrations/` lists 001–008. The single deviation from design §8's SQL listing — the extra `preview jsonb` column on `portability.portability_imports` — is documented as adaptation O1 in `decisions.md`. `database.CurrentSchemaVersion = 8` and the smoke migration pin moved 7→8 (`migration_test.sh` diff is the two-line pin change only).
- **No real-provider egress in CI.** Smoke `static_test` asserts `APP_ENV=development`, `MODEL_ADAPTER=deterministic`, `REMINDER_EMAIL_ADAPTER=fake`, `REMINDER_SMS_ADAPTER=fake` in `.env.example`; the private-mode block runs `APP_ENV=development` + `DEV_INBOX_ENABLED=true` with the compose fake-adapter defaults (assumption A7, red line preserved); compose defaults remain fake/deterministic everywhere; `.github/workflows/ci.yml` is unchanged in the diff (no gate lowering, no provider env).
- **No credentials committed.** `scripts/check-secrets.sh` exits 0 (also ran as the final `make verify` step). Manual pattern scan of every added diff line found only placeholders: `deploy/private/env.template` `POSTGRES_PASSWORD=change-me` plus empty `MODEL_API_KEY=` / `REMINDER_SMTP_PASSWORD=` / `REMINDER_ALIYUN_ACCESS_KEY_SECRET=` template slots, and the `postgres://user:secret@db/workbench` fixture string in `config_iter0004_test.go` (same pattern as the pre-existing ITER-0003 config test). The backup output dir and `.artifacts/` are gitignored.
- **Architecture policy.** `make architecture-test` green inside gate 2, including the new `invalid-cross-context-portability` fixture asserting the `go-cross-context` rule catches portability importing another context's domain; `TestRepositoryHasNoArchitectureViolations` passes on the real tree.

## 5. Out-of-scope assessment

Task 17's three documented out-of-scope fixes (commit `5b8b752`, recorded as O5 in `decisions.md`) were audited hunk-by-hunk:

1. `backend/cmd/api/main.go` — empty-schema fail-safe restored: `RequireSchema` failure now logs and serves degraded (liveness up, readiness 503) instead of `os.Exit(1)`; startup provisioning (instance identity + private admin) runs only after schema verification succeeds. Matches O5.1 exactly; no further behavior change.
2. `backend/cmd/api/wiring.go` — `channelImportShim` pre-checks duplicates with an in-transaction listing and returns the existing id + `ErrChannelExists` sentinel before any insert, keeping confirm's single UoW transaction-clean. Matches O5.2 exactly.
3. `backend/internal/modules/reminder/adapters/outbound/postgres/deliveries.go` — `scanDelivery` scans `plan_id` into `*string` (mirroring `scanDeliveryWithOrigin`) so delivery `List` paths survive plan-less imported rows; the +15 test lines in `integration_test.go` are the documented regression coverage. Matches O5.3 exactly.

None of the three reaches beyond what `decisions.md` documents. The other adaptations were likewise verified against the diff: O1 (preview column, `7dadb92`), O2 (403 mapping, `1aa697e`), O3 (`globals.css` change is styling-only `.data-*` classes on the existing palette, plus `.data-error` joining the existing error-class list), O6 (smoke asserts the T9 downgrade contract instead of the brief's unattainable "copied channels appear unverified" wording for a self-import — the fresh-workspace path is covered by `TestPortabilityRoundTripBetweenTwoWorkspaces`).

Every file in `git diff --name-status 05dd59d..HEAD` maps to the plan's File and Module Map, its yellow-zone register, or the documented adaptations above (the `.gitignore` additions realize A5/Task 16's gitignored-artifact notes). No other out-of-scope change was found.

**Observations (non-blocking):**

1. `TEST_DATABASE_URL` was absent from the reviewer's shell although the dispatch brief said it would be set; the gates were therefore run exactly as CI runs them, and DB-backed proof came from `make migration-test`'s real-DB adapter/composition proof. Recorded for transparency; not a defect.
2. Criterion 5's "re-confirm returns 409 与既有报告": the 409 body is the stable `ErrorEnvelope` (`{code: import_conflict, message, correlationId}`), matching the design's own §7.1 route table and the frozen `portability.yaml` 409 schema; the existing report is preserved and retrievable via `GET /imports/{importId}` (the smoke asserts both). Design §7.2's prose and §7.1's table differ on whether the report rides in the 409 body; the implementation follows the normative frozen contract, so this is judged PASS with the nuance recorded rather than silently absorbed.

## 6. Verdict

All four gates exit 0 when re-run from a clean context at the branch tip; all 10 acceptance criteria map to concrete, re-verified evidence; the diff stays inside the declared zones with zero new dependencies, migrations 001–007 untouched, no real-provider egress in CI, no committed credentials, and architecture policy green; the documented out-of-scope fixes match their hunks and nothing exceeds them.

VERDICT: PASS
